"""vdrum: headless webcam air-drum detection core.

Pure detection side (PLAN.md). The ported core is `detect.py`; the MediaPipe
wrapper is `tracker.py`; the expensive video->track stage is `extract.py`.
"""

__version__ = "0.1.0"
